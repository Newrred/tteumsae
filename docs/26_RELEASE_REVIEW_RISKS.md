# 오전 11시 팀 검토: 출시 위험과 공유 범위

기준: 2026-09-13. 최신 검토 대상은 **0.13.1 / code34**, 아래 기존 독립 감사는 **0.13.0 / code33** 기록이다.
이번 문서는 새 기능 제안이나 심사 승인서가 아니라, 팀이 최종 제출 여부를 판단하기 위한 점검표다.

## 최신 code34의 검증 범위와 남은 위험 — code33 감사와 분리

최종30E0는 수동·자동 보존 각각227/227·51 suites, 수동 lint 오류0/경고48와 서명 빌드를 통과했다.
최종 주소/시트 재실행1/1(73초) 및 묶음 핀의 시트 가림·상단 버튼 겹침 해소를 실기기에서 확인했다.
첫 목적지 검색은 공급자 응답 지연으로58초 실패했고 같은 검색 재시도로 복구했다. 일시적 외부 오류는
실제로 발생 가능한 위험이며, 마지막 깨끗한 재실행 통과만으로 항상 성공한다고 보장하지 않는다.
선택·상세·알림 토글·스크롤 검사는 E55 후보, 큰 글자 검사는220개 후보 이력이다.
최종30E0의 전체 앱·모든 글자 크기 검사나 새 알림 실수신 검사로 확대하지 않는다.
상세 왕복의 카메라 위치는 보존하지 않는다. 최종 파일·증거는 [보완 기록](27_RELEASE_UX_REFINEMENT.md)을 따른다.
주소 backend는265/265·검사121개 후 운영 반영됐다. **06:39 공개 code34 배포·익명 재다운로드 해시 일치를 확인**했다.
포털은 마지막 확인 code32, 기존11시묶음은code33이다. 주소 backend는 `8e3ae42`,
Android/UI·검증 문서·다운로드 페이지는 **`d877f32`로 origin/main 반영 완료**다.
새 검토본의 최종 파일과 기존 자료를 [code34 변경 안내](28_CODE34_REVIEW_ADDENDUM.md)로 구분한다.
이전 PDF/PPTX·권리자료·판매 이미지가 전부34로 갱신되거나 제출됐다는 뜻은 아니다.
최종 신청은 여전히 11시 팀 검토 후 사용자의 새로운 명시 지시가 필요하다.

## 기존 code33 독립 감사 결론 — 당시 기록

이번 읽기 감사에서 새로 확인한 앱 코드의 P0/P1 결함은 없다. 다만 **원스토어 최종 심사 신청은 보류**다.
오전 11시 팀 검토 후 사용자의 새로운 진행 지시를 받아야 한다. 이전의 포괄 제출 승인을 근거로 먼저 신청하지 않는다.
스토어 등록 중, APK 직접 다운로드 가능, 심사 접수, 심사 승인, 스토어 공개는 각각 다른 상태다.

## 제출 전 반드시 맞출 항목

| 항목 | 현재 확인과 팀 결정 |
|---|---|
| 최종 진행 승인 | 팀이 앱과 자료를 검토한 뒤 사용자에게 최종 심사 신청 여부를 명시적으로 확인한다. |
| 원스토어 파일 일치 | 포털의 기존 code32 APK를 최신 검증 code34 APK로 교체하고, 대상 버전을 맞춘 권리 자료·판매 이미지를 대조한 뒤 저장 결과를 재조회해야 한다. 국가 저장 등 나머지 입력 상태는 최종 포털 확인 기록을 따른다. 아직 완료로 추정하지 않는다. |
| 공모전 배포 링크 | 실제 ONE store 공개 링크는 아직 없다. 직접 APK 페이지나 등록 상품번호를 스토어 공개 링크로 제출하지 않는다. 외부 심사 기간 때문에 일정 위험이 남는다. |
| 참가·API 정보 | 팀명 자투리와 최종 명단·대표자·공모전 신청 정보를 대조한다. TourAPI 본인 신청은 사용자 확인을 받았지만, 실제 운영 키·신청 계정·제출 증빙의 대조 완료로 확대하지 않는다. 키 원문은 이 검토 자료에 넣지 않는다. |
| 권리·정책의 한계 | 권리 설명 PDF는 개별 허가서나 원스토어 인정 완료 증거가 아니다. 수동 출발지 구조 역시 법적 신고 면제·심사 통과를 보장하지 않는다. 추가 증빙 요구가 나오면 별도로 대응한다. |

## 기존 code33에서 검증된 범위 — 새 빌드 전체 검증으로 확대하지 않기

- 수동·자동 보존 빌드 각각 Android 195/195, 44개 테스트 묶음 통과. 수동 lint는 오류 0 / 경고 48.
  자동 빌드는 보존·검증용이며 배포 대상이 아니다.
- 최종 수동 APK에는 기기 위치 권한이 없다. 장소 검색으로 선택한 좌표는 서버와 지도 제공자에게 전달된다.
  교통 재확인은 실제 이동한 현재 위치가 아니라 선택한 출발지 기준이다.
- 최종 업데이트 설치에서 카카오 로그인·기존 알림을 유지했다. 관광 경로 입력·추천·상세·외부 지도,
  저장·탐색·이용 고지와 접힌 검색 지도의 카카오 로고를 실기기로 확인했다.
- 실제 출발 알림은 예약 04:33보다 늦은 **04:35:59**에 도착했고, 권장 출발 04:38 전에 알림을 눌러
  경포호수광장 → 경포대 경로로 연결했다. **정시 알림 검증 통과가 아니다.**
- 공개 APK를 별도로 다시 읽어 SHA-256을 대조했고 최종 로컬 검증 해시와 일치했다.
  `895F46C5AF53EF24A8E38A4A73DD4D4F3B660C87EEAE0A667F90D21917F6AFB6`
- [APK 안내](https://tteumsae-apk-six.vercel.app/),
  [서버 상태](https://tteumsae-backend-one.vercel.app/api/health),
  [개인정보처리방침](https://tteumsae-backend-one.vercel.app/privacy),
  [계정 삭제 안내](https://tteumsae-backend-one.vercel.app/account-deletion)는 이번 감사에서 HTTP 200을 확인했다.
  페이지 응답 성공이 모든 기능의 성공이나 법적 적합성 검증을 뜻하지는 않는다.

## 아직 확인하지 않았거나 보장하지 않는 부분

- 최신 출시본의 Google 재로그인, 실제 계정 삭제, 네트워크 차단 상태의 전체 흐름은 실기기 검증에 포함하지 않았다.
  기존 사용자 계정을 검증 목적으로 삭제하지 않는다.
- 예전 GPS 앱에서 데이터를 보존한 채 수동 버전으로 업데이트하는 전체 과정을 실증하지 않았다.
  모드별 복원 격리는 단위 테스트로 확인했고, 기존 debug 앱은 사전 승인 후 삭제·재설치했다.
- 여러 기기·Android 버전·큰 글자·장시간 절전·재부팅 이후 알림까지 모두 실증한 것은 아니다.
  알림은 늦거나 출발시각 이후 만료되어 표시되지 않을 수 있다. 사용자가 출발시각을 직접 확인해야 한다.
- 실제 차량 운행, 현장 영업·대기시간, 전체 관광사진의 개별 권리, 모든 장소 정보의 최신성을 확인한 것은 아니다.
  이동·체류시간은 예상값이고 날씨·집중률·주차는 방문 보조 정보다.
- 기본 Maestro 전체 폴더에는 GPS 전용 흐름이 남아 있다. 수동 버전 QA는 smoke 태그나 파일을 지정한다.
  특정 시각·로그인·저장 상태에 의존하는 9개 촬영 도구는 일반 smoke에서 분리했다.

## 기존 code33 묶음 안의 자료 버전이 달라 보일 때

- 아래는 기존 묶음의 설명이다. 최신code34 자료 대조는 [변경 안내](28_CODE34_REVIEW_ADDENDUM.md)를 따른다.
- 기존 묶음 APK와 권리 PDF는 code33이다. 공모전 기능설명서는 `code32-review-v3`라는 파일명을 유지한다.
  code33 변경은 접힌 검색 지도 로고의 위치뿐이고 해당 문서의 펼친 입력·추천 화면은 바뀌지 않았다.
- 기능설명서의 관광 화면은 code32, 동일한 홈·설정 상단은 code31 실기기 캡처다. 모두 수동 0.13.0이며
  과거 GPS 화면을 사용하지 않았다. 이 촬영 버전 구분을 숨기거나 모든 컷을 code33 촬영으로 바꾸어 쓰지 않는다.
- 공식 원본 9개 양식 중 핵심 기능 흐름 페이지를 허용된 복제 방식으로 3종 작성해 총 11쪽이다.
  공식 양식은 핵심 기능 최대 5개와 필요 시 페이지 복사를 안내한다.
- `19_CONTEST_FUNCTIONS_DRAFT.md`와 예전 QA 기록은 작성 이력이다. 최신 상태표 대신 통째로 공유하지 않는다.

## 공유 폴더에 넣지 않는 것

공유는 준비된 최종 APK·PNG·PDF·PPTX·검토 문서만 허용 목록으로 복사한다. 작업 폴더 전체를 압축하지 않는다.

- `output/private-signing-backup-20260913/`, 서명키(`.jks`, `.keystore`), `keystore.properties`, 비밀번호·복구 코드.
- `.env*` 실제값, `local.properties`, `.vercel/`, OAuth/CLI 인증 캐시, 서비스 역할 키·토큰·API 키 원문.
- 알림창·계정·설정 화면 XML/덤프, 원시 로그, 로그인 세션·쿠키, 폰 전체 화면·진단 묶음.
- 자동 위치 APK, debug APK, 오래된 code31/32 APK, 실패·로딩 중·마트 예시·다른 시각을 섞은 캡처.
- 임시 정책 문서·임시 권리 PDF·구버전 발표 파일을 최종본처럼 포함하지 않는다.

### 실제 공유 묶음 독립 검수 - 2026-09-13 05:00 KST

`output/team-review-20260913-1100`의 실제 28개 파일을 조립 후 별도로 검수했다.
누락·추가·크기/SHA-256 불일치·심볼릭 링크는 없었고 원본 파일 해시도 일치했다.
Markdown 상대 링크가 모두 연결됐으며 PDF는 기능설명서11쪽/권리근거5쪽/검토요약2쪽이다.
스토어 화면6장557×1300, 아이콘512×512, 배너1024×578을 확인했다.
문서·발표 내부 텍스트와 이미지 메타데이터에서 비밀키·로그인토큰·개인 전화번호 패턴이
검출되지 않았고 PNG 위치 EXIF도 없었다. 선별 증거4장은 실제 이미지 검수에서 계정·알림센터 노출이 없었다.
ZIP 내부도 같은 허용 목록과 파일별 SHA-256을 대조했다.

이는 정해진 검사 범위의 결과이지 모든 바이너리의 비밀정보 부재를 보증하는 것은 아니다.
APK는 승인된 기존 최종 파일의 해시 일치로 검증했고 공개 클라이언트 설정은 서버 비밀키와 구분한다.
인증서 지문·APK 해시와 공개 승인된 문의 메일은 비밀키가 아니며 검토 자료에 포함한다.
최종 커밋 후 재생성한 묶음의 기준 Git·파일별 해시는 MANIFEST를 따른다.

근거: `08_QA_AND_KNOWN_ISSUES.md`, `16_RELEASE_SPRINT_2026-09-13.md`,
`17_ONESTORE_SUBMISSION_DRAFT.md`, `18_LOCATION_BUILD_MODES.md`,
`21_THIRD_PARTY_RIGHTS_EVIDENCE.md`, 공식 기능설명서 원본과 최종 발표 README.
