# 원스토어 출시 점검 및 제출 자료 초안

## 현재 등록 상태 — 2026-09-13 04:05 KST

- 상품 `0001009120`: 등록 중. 수동 **0.13.0/code32** APK 업로드 완료(포털 수신03:48:33).
- 최종 로컬/공개 다운로드 APK SHA-256:
  `70018291A0E36661DC9D36EB685BD9C9FE952AAAAA0EB02E1672A7091E91575D`.
- 외부결제 미사용: 변경 제한 고지를 사용자 확인 후 확정. 인앱상품·광고는 넣지 않았다.
- 공통정보: 실제 처리 항목 입력 및 5쪽 지식재산 이용 근거 PDF 첨부 완료.
  일반 저장 확인창에서 브라우저 응답이 지연돼 저장 완료는 아직 확인하지 못했다.
- 사진/OSS 고지는 code32에 반영, Android190/190·lint0오류/48경고·실제 설치 검증 완료.
- IARC 이메일 제공은 사용자 동의 완료. 새 약관 확인·등급 설문은 사용자 직접 진행 요청.
  아직 최종 심사 신청·승인·공개 아님.
- 공개 개인정보 안내는 수동0.13.0/미국 Vercel/서울 Supabase 실제 구조로 갱신 완료.
- 관광지 경유(안목해변→순포해변→경포대) 및 탐색/홈/입력 총6장 업로드,
  판매정보 최종 저장은 미확인. 내부 마트 QA는 제출 이미지에서 제외했다.
- 제3자 권리 근거는 `21_THIRD_PARTY_RIGHTS_EVIDENCE.md`. 해당 문서를 개별 허가서나
  원스토어 증빙 충족 승인으로 표현하지 않는다.

아래 원래 초안의 과거 기준선·계획은 위 현재 상태와 구분한다.

작성일: 2026-09-13 (KST)

과거 점검 기준: `main` `6218b20`, Android `0.12.9` / code `30`

현재 제출 목표: 출발지를 직접 검색·선택하는 **수동 출발지 출시본**. 자동 위치 기능은 별도 빌드로 보존한다.

상태: **등록 자료 초안 / 일부 필수 입력 대기**. 4~6절은 수동 출시본의 설명이며, 3절의 0.12.9 APK 감사는 과거 기준선이다.
최종 APK·운영 연결·관광 캡처는 확정했지만 알림 실제 수신 등 남은 QA와 심사 접수·승인을 완료한 문서는 아니다.

## 1. 사용자 승인으로 확정한 등록 기준

| 항목 | 값 |
|---|---|
| 앱 이름 | 틈새 |
| 패키지 | `com.tteumsae.app` |
| 마켓 | ONE store (삼성 Galaxy Store·Google Play와 별개) |
| 원스토어 상품 | `0001009120` / 등록 중 (root 포털 확인, 심사 접수·공개 완료 아님) |
| 가격·지역 | 무료 / 국내 |
| 현재 광고·앱 내 결제 | 없음 |
| 로그인 | 핵심 기능은 비로그인 사용, 계정·프로필은 선택 로그인 |
| 출발지 설정 | 장소 검색 결과에서 직접 선택. 제출 목표 빌드는 기기 현재 위치를 취득하지 않음 |
| 공개 운영자 | 신홍 |
| 공개 문의 메일 | godburgundy@gmail.com |
| 개인정보처리방침 | https://tteumsae-backend-one.vercel.app/privacy |
| 계정 삭제 안내 | https://tteumsae-backend-one.vercel.app/account-deletion |

사용자는 검증된 출시 수정의 GitHub main 반영, 기존 Vercel 운영 서버·APK 페이지 배포,
원스토어 자료·APK 업로드와 심사 신청 및 승인 후 공개를 승인했다.
새 약관 동의, DB 구조 변경, 새 Cron, 유료 서비스, 비밀값 공개는 이 승인에 포함하지 않는다.
광고 SDK는 이번 출시에서 제외하고 추후 별도 도입을 검토하기로 했다.
자동 위치 기능도 필요한 절차·정책·검증을 갖춘 별도 새 버전으로만 배포한다. 원격 설정으로
심사 이후 현재 위치 기능을 켜는 구조가 아니다.

## 2. 오전 4시 전 사용자 확인이 필요한 항목

1. **최종 release 앱 실기기 전환:** 과거 감사에서 개발용 앱과 기존 release의 서명 차이를 확인했다.
   사용자는 02:18경 개발용 앱 삭제 후 배포용 앱 재설치를 승인했다(스프린트 기록 참조).
   승인과 실제 설치 완료는 다르므로 실행 결과·최종 버전/해시는 별도 기록한다.
2. 원스토어 회원가입·로그인은 사용자 완료. 이후 새 약관·본인인증·판매자 정보 확인이
   추가로 필요한 경우 그 단계에서 사용자가 직접 확인한다.
3. 카카오·Google 로그인 재인증이 필요하면 사용자가 직접 진행한다.
   현재 release 서명이 카카오 플랫폼에 등록됐는지는 최종 release 지도로 검증한다.
4. 서명키의 독립 보안 백업 위치와 복구 책임자 확인. 키·비밀번호는 문서·Git에 넣지 않는다.
5. 공모전 신청 계정·팀 최종 명단·실제 사용한 TourAPI 키의 소유권과 안전한 제출 경로 확인.
   Git에는 활용신청 관리 계정이 기록돼 있지 않아 소스만으로 소유자를 확정할 수 없다.
   문의 메일 공개 승인은 받았지만 메일함 접근·이메일 발송 권한으로 확대 해석하지 않는다.
6. 사용자에게 위치기반서비스사업 신고 이력·신고필증이 없음을 확인했다. **신고 진행 중이나
   심사 대기 중으로 쓰지 않는다.** 이번 제출 목표는 기기 GPS를 사용하지 않는 수동 출발지
   빌드이며, 원스토어에는 그 최종 APK의 실제 권한·데이터 흐름으로 설명한다.
   수동 구조가 법적 신고 의무나 원스토어 증빙 요구의 면제를 확정한다는 뜻은 아니다.
   공식 검토 출처: https://onestore-dev.gitbook.io/dev/help/faq/review

## 3. 과거 APK 감사와 현재 출시 변경의 구분

### 수동 출시본에 반영한 범위 — 최종 code32 확인

- 기본 빌드는 `manual`, 자동 위치 공급자와 권한은 별도 `automatic` 빌드로 분리한다.
- 수동 빌드에서는 현재 위치 버튼·위치 권한 요청·자동 출발지 채우기·GPS 역지오코딩을 사용하지 않는다.
- 출발지와 목적지는 사용자가 장소 검색 결과에서 선택한다. 선택 장소의 좌표를 서버에 보내
  경유지·차량 경로를 계산한다. 따라서 **모든 좌표의 서버 전송이 없는 앱**으로 설명하지 않는다.
- `현재 교통으로 다시 확인`은 선택한 출발점과 기존 목적지·도착 마감을 기준으로 재계산한다.
  사용자가 실제로 이동한 위치를 새로 취득하거나 추적하는 기능이 아니다.
- 이전 자동 위치 버전의 입력·알림 좌표가 수동 모드로 복원되지 않게 모드 경계를 적용하고
  단위 회귀를 통과했다. 기존 GPS 데이터 보존 업데이트의 실증은 하지 않았다.
  저장 장소와 선택 로그인 등 나머지 기능은 유지한다.
- 제출 소개·스크린샷·정책·검증팀 메모는 최종 수동 APK와 일치시킨다.

### 0.12.9 과거 기준선의 권한·광고·로그인 감사

- `android/app/build.gradle.kts` 직접 의존성과 Android 소스에서 AdMob, 광고 ID,
  AppLovin, Unity Ads, Meta Audience Network, 결제 SDK, Firebase Analytics 사용을 찾지 못했다.
- 폰에서 읽어온 0.12.9 APK의 실제 권한은 `INTERNET`, `ACCESS_COARSE_LOCATION`,
  `ACCESS_FINE_LOCATION`, `POST_NOTIFICATIONS`, `RECEIVE_BOOT_COMPLETED` 및 AndroidX의
  앱 내부 receiver 보호용 권한이다. `AD_ID`, 결제, 연락처·카메라·마이크·광범위한 저장소,
  백그라운드 위치·정확한 알람 권한은 포함되지 않았다.
- 현재 위치는 사용자가 요청할 때 가져오고 요청 종료/취소 때 위치 업데이트를 해제한다.
  위치 거부 시 출발지를 직접 검색할 수 있다. 알림은 별도 선택 기능이다.
- 시작 화면은 HOME이며 인증 상태로 검색·추천·상세를 차단하지 않는다. 로그인은 설정의
  계정 영역에서 선택한다. 장소 저장은 `RoomSavedPlacesRepository.toggleGuest`로 처리한다.
- 앱이 직접 제공하는 저장 장소의 계정 간·기기 간 동기화는 없다. 프로필만 선택 로그인
  이후 Supabase에 저장하며 계정 삭제 기능이 존재한다. 실제 계정 삭제는 점검하지 않았다.

### 서명·설치 상태

| 대상 | 확인 결과 |
|---|---|
| 감사 당시 폰 | Samsung SM-F776N, 0.12.9/code 30, DEBUGGABLE |
| 폰 APK 인증서 | `CN=Android Debug`, SHA-256 `a770da06e606dfcfd793b33a6076c23bd91f403d342aa625b7064f7ef3150fda` |
| 기존 release APK | 2026-09-05 산출물, 44,808,929 bytes, 최신 출시본 아님 |
| 기존 release 인증서 | `CN=Tteumsae Upload Key`, SHA-256 `f43f06a02493bac8974c6cc8d37411eb66b436dd1f568eee9f5be8e56f2f26b7` |
| 로컬 서명 준비 | `android/keystore.properties`, 필수 4개 값, 지정 keystore 파일 존재만 확인 |

두 APK는 `apksigner verify --print-certs`로 검증했다. 인증서 지문은 비밀키가 아니다.
현재 keystore와 기존 release APK 인증서의 동일성은 위 존재 점검만으로 추정하지 않는다.
최종 새 release APK의 서명·버전·지도 동작을 별도 확인해야 한다.
폰에서 복사한 APK는 Git 제외 `tmp/release-audit-20260913/installed.apk`에 있고 제출용이 아니다.

### 과거 정책 감사와 현재 보완 결과

- 과거 `backend/privacy.html`에는 “실제 운영 전 … 방침에 반영”이라는 미완성 문구가 있었다.
  03:39경 실제 수동 코드·Vercel 미국·Supabase 서울 처리와 대조한 안내를 운영에 배포했다.
  공급자 보안 로그까지 즉시 완전 삭제된다거나 법적 적합성 검토가 끝났다는 뜻은 아니다.
- `ExternalSettings.kt`의 `LOCATION_TERMS_URL`은 빈 값이다. 준비되지 않은 공개 문서를
  실제 제공하는 것처럼 소개하지 않는다.
- 과거 기준선은 shared preferences만 백업에서 제외했다. 이번 스프린트 소스에는 Room DB도
  백업·기기 이전에서 제외하는 변경이 반영됐다. 최종 APK 포함 여부는 빌드 검증으로 확정한다.
- 출발 알림은 `setAndAllowWhileIdle` 기반으로 정확한 시각의 수신을 보장하지 않는다.
  최종 설명은 검증된 알림 동작·정확성 수준에 맞추며 정시 도착 보장으로 표현하지 않는다.
- 기존 문서의 Play App Signing, Play 테스트 트랙, 첫 AAB 업로드는 Play 전용 과거 항목이다.
  원스토어 출시의 선행 완료 조건으로 무조건 적용하지 않는다. 원스토어의 실제 등록 화면과
  제출 바이너리 요구를 확인하고 그 마켓이 전달하는 최종 서명에 맞춰 지도 검증을 수행한다.

## 4. 스토어 짧은 소개 초안

도착 시간까지 남은 여유, 강원 여행길에 들를 한 곳을 찾아보세요.

## 5. 스토어 상세 소개 초안

약속까지 시간이 조금 남았을 때, 어디에 들르면 좋을까요?
틈새는 출발지와 목적지, 도착해야 할 시각을 바탕으로 강원 여행길의 경유지 한 곳을 찾도록 돕습니다.

**이동 경로와 남은 시간을 함께 살펴보세요**

출발지와 목적지를 검색해 선택하고 도착 시각을 정하면 차량 이동 경로 주변의 장소를 제안합니다.
후보별 추가 이동시간과 계산 기준 최대 체류시간을 비교하고, 방문할 한 곳을 선택할 수 있습니다.
마음에 드는 후보가 없다면 목적지로 바로 가는 경로도 확인할 수 있습니다.
출발 장소를 직접 정하기 때문에 여행 전에도 원하는 구간을 살펴볼 수 있습니다.

**지도와 목록을 오가며 비교하세요**

지도에서는 장소 종류를 아이콘으로 살펴보고, 목록에서는 시간 정보와 장소 정보를 비교할 수 있습니다.
선택한 장소의 상세 정보를 확인한 뒤 카카오맵으로 길 안내를 이어갈 수 있습니다.

**방문에 도움이 되는 정보를 확인하세요**

한국관광공사 관광정보를 바탕으로 사진, 주소, 운영시간 등 확보된 정보를 안내합니다.
장소에 따라 주차 정보, 주변 공영주차장, 접근성 안내, 날씨와 관광지 혼잡 예측도 확인할 수 있습니다.
제공되는 정보의 종류와 범위는 장소마다 다릅니다.

**마음에 든 장소는 저장하세요**

장소를 둘러보고 저장해 다시 찾아볼 수 있습니다. 검색·추천·장소 저장은 회원가입 없이 이용할 수 있으며,
카카오·Google 로그인은 계정과 프로필 관리가 필요한 경우에만 선택하면 됩니다.
현재 앱 자체의 저장 장소 계정 동기화는 제공하지 않습니다.

**이용 전 확인해 주세요**

- 현재 강원 지역 목적지와 자동차 이동 중심으로 제공합니다. 실제 추천 범위는 확보된 장소 데이터에 따라 다릅니다.
- 이동시간과 체류시간은 계산 당시 교통·운영 정보에 따른 참고값이며 정시 도착이나 실제 영업을 보장하지 않습니다.
- 방문 전 운영 여부·입장 마감·이용 제한을 확인해 주세요. 주차장 거리는 별도 표시된 직선거리이며 실시간 빈자리 정보가 아닙니다.
- 관광지 혼잡 정보는 상대적인 예측값, 날씨는 격자 예보이며 현장 실황과 다를 수 있습니다.
- 인터넷 연결이 필요합니다. 외부 길 안내는 카카오맵 앱 또는 연결 페이지를 이용합니다.
- 이 버전은 기기 현재 위치를 사용하지 않고 선택한 출발지를 기준으로 계산합니다.
  재확인도 같은 출발지 기준이므로 실제 출발 장소가 달라졌다면 입력을 수정해 주세요.
- 출발 알림은 선택 기능이며 알림 권한을 거부해도 검색·추천·길 안내를 이용할 수 있습니다.
  기기 절전 설정 등에 따라 알림이 늦어질 수 있습니다.

문의: godburgundy@gmail.com / 운영자: 신홍

## 6. 검증팀 참고사항·테스트 절차 초안

핵심 기능은 로그인 없이 이용할 수 있어 공용 테스트 계정이 필요하지 않습니다.
이 제출 버전은 기기 현재 위치 취득과 위치 권한 요청 없이 출발지를 직접 검색·선택합니다.
사용자가 선택한 장소 좌표로 서버에서 경로를 계산합니다. 실제 차량 운행은 필요하지 않습니다.

1. 틈새를 실행하고 홈 지도와 안내를 확인합니다.
2. 목적지 검색으로 들어가 출발지 `강릉역`, 목적지 `경포대`를 검색해 각각 결과를 선택합니다.
3. 도착 시각은 **검사하는 현재 시각보다 약 2시간 이후**로 설정합니다. 관심 필터는 선택하지 않아도 됩니다.
4. 틈새 찾기를 눌러 결과를 확인합니다. 운영시간과 현재 시각에 따라 후보 수·장소는 달라집니다.
   무결과인 경우 입력을 수정하거나 목적지 직행을 선택할 수 있습니다.
5. 후보 한 곳을 선택해 체류·출발 안내를 확인하고 지도/목록 왕복 및 상세 보기→뒤로를 확인합니다.
6. 카카오맵 길 안내를 눌러 출발지·경유지 한 곳·목적지 전달을 확인합니다. 미선택 시 직행 경로를 확인할 수 있습니다.
7. 장소 탐색에서 장소를 저장·해제하고, 설정에서 개인정보처리방침과 문의 방법을 확인합니다.
8. 선택 로그인까지 검사하려면 설정→계정에서 카카오 또는 Google의 검증 담당자 소유 계정으로 로그인합니다.
   로그인은 추천 필수 단계가 아니며 개인정보가 포함된 실제 운영자 계정을 테스트 계정으로 제공하지 않습니다.
9. 재확인을 눌러 선택한 출발지·목적지 기준의 결과 갱신을 확인합니다. 기기 현재 위치로
   출발지가 자동 변경되지 않는 것이 이 버전의 정상 동작입니다.

최종 release 빌드에서 위 절차를 다시 실행한 뒤 검증 메모로 사용한다. 이 절차 자체가 통과 기록은 아니다.
알림은 실제 예약 후 수신·탭까지 확인하고, 앱 데이터 삭제·로그아웃·실제 계정 삭제는 사용자 승인 없이 수행하지 않는다.

## 7. 등록 이미지·공모전 자료 현황

| 자산 | 현재 위치·판단 |
|---|---|
| 사용자 지정 스토어 로고 | `C:\Users\shjb0\Documents\카카오톡 받은 파일\KakaoTalk_20260908_220323636.png` (사용자 전달 1024 PNG, 이번 점검에서 파일 존재 확인) |
| 과거 런처 아이콘 기준선 | `android/app/src/main/res/drawable/ic_launcher.xml` (공식 전달 PNG 적용 전 벡터) |
| 디자인 참고 원본 | `design/design-system-v1.0.svg` |
| 기존 화면 QA 이미지 | `android/app/build/maestro-visual-results/2026-09-05_173954/` 및 `android/app/build/ux-audit/` |
| 이전 시각 리포트 | `android/app/build/maestro-visual-results/report.html` |
| 9/11 새 PC 캡처 | QA 문서에 `tmp/ux-audit-20260911/`로 기록돼 있으나 이 PC에서는 이미지 파일을 찾지 못함 |
| 스토어용 최종 스크린샷 PNG | 이번 점검에서 확인하지 못함. 최종 release 화면을 새로 캡처해야 함 |

이전 QA 이미지에는 개인정보가 포함될 수 있고 0.12.9와 다른 화면이므로 그대로 등록하지 않는다.
사용자 지정 공식 로고 반영 작업의 결과는 최종 launcher와 스토어 이미지에서 확인한다.
아이콘 PNG 크기·스크린샷 규격·필수 개수는 원스토어 실제 등록 화면의 요구를 확인한 뒤 제작한다.
공모전용으로도 같은 최종 화면을 재사용하되, 대표 1장과 기능 흐름이 보이는 상세 3~5장을 준비한다.

추천 캡처 구성: 홈 / 출발·목적·도착 시각 입력 / 결과 목록 선택 / 결과 지도 / 실제 정보가 있는 상세.
개인 프로필·알림 내용·현재 집 위치가 화면에 섞이지 않는 공개 장소 경로를 사용한다.

## 8. 공모전 API 활용 기재용 사실 초안

| 제공 정보 | 실제 구현·운영 범위 | 과장하면 안 되는 표현 |
|---|---|---|
| 국문 관광정보 | `KorService2` 장소 목록·공통·소개·사진·반복 안내·반려동물 상세 처리 코드, 확보 데이터 조건부 표시 | 모든 장소의 모든 정보가 완비됨 |
| 관광지 집중률 | 강릉 exact unique name으로 연결된 장소의 날짜별 상대 예측 | 실시간 인원·실시간 혼잡 |
| 무장애 여행 | `KorWithService2`와 content ID가 일치하는 보강 장소의 접근성 안내 | 모든 장소의 접근성 보장 |
| 기상청 단기예보 | 대상 야외 장소 도착 무렵의 격자 예보 | 현장 실황·방문 가능 보장 |
| 공영주차장 | 강릉 공급 데이터 중 장소 반경 1km 내 최대 3곳, 직선거리 | 실시간 빈자리·장소 전용 주차장 |
| 카카오 | 네이티브 지도, 장소 검색·선택 장소 지역 확인, 차량 경로, 외부 길 안내, 선택 로그인 | 기기 현재 위치 자동 입력·앱 자체 내비게이션·도착 보장 |

API 연결 수를 성과로 과장하지 않고 실제 데이터가 표시되는 화면과 선택에 도움을 주는 지점을 설명한다.
공모전 공식 지정 기능설명서 양식의 PDF가 필요하며, 이 Markdown 초안이 해당 제출 서류를 대신하지 않는다.
실제 제출 키·로그인 토큰·서명 비밀값은 이 문서에 넣지 않는다.

## 9. 원스토어 검증 일정에 대한 확인 근거

원스토어 공식 문서에 따르면 공개 배포 전 검증이 필요하고 영업일 기준 최대 5일,
예외적으로 더 걸릴 수 있다. 검증 완료 후 즉시·직접·예약 적용을 선택할 수 있으며
검증 중에는 앱 정보를 수정할 수 없다. 따라서 가입 완료나 APK 업로드 완료를 공개 링크 확보로 기록하지 않는다.

공식 근거: [원스토어 검증 요청하기](https://onestore-dev.gitbook.io/dev/docs/apps/request-for-review)
(2026-09-13 확인).

최종 완료 기록은 별도 스프린트 문서에 실제 Git 커밋·APK 버전/해시·실기기 결과·원스토어 접수 상태와
시각을 기록하고, 검증 중/공개 완료를 구분한다.
