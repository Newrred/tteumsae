# 결정 기록

이 문서는 구현 선택의 이유를 짧게 남깁니다. 이미 확정된 사실과 아직 결정하지 않은 항목을 구분합니다.

## 기록 형식

```text
## YYYY-MM-DD — 제목
상태: 제안 / 확정 / 폐기
결정:
이유:
영향:
다시 검토할 조건:
```

## 2026-08-28 — 감사 후 제품·구현 우선순위 재정렬

상태: 확정

결정:

- `00_START_HERE`, `03_FEATURE_MATRIX`, `04_SCREEN_FLOWS`는 현재 구현 사실만 기록한다.
- 제품 목표는 `01_PRODUCT_AND_SCOPE`, 이 결정 기록과 최신 도착 마감 스펙을 기준으로 한다.
- 신규 시간 계약은 상대 `deadlineMinutes`가 아니라 절대
  `arrivalDeadlineEpochMillis`를 보낸다. 서버가 수신시각 기준으로 남은 시간을 내림
  계산해 네트워크 지연이 마감을 뒤로 미루지 않게 한다.
- 관심 조건은 별도 필수 단계가 아니라 선택 필터로 둔다.
- 첫 알파는 경유지 한 곳만 완료 흐름으로 제공한다. 제품상 최대 2곳 원칙은 유지하되,
  두 번째 장소는 한 곳 흐름의 실제 성과를 확인한 뒤 실험한다.
- TourAPI 전체 보강보다 운영시간 신뢰의 얇은 기준선과 도착 마감 핵심 플로우를 먼저 완료한다.
- `TteumsaeApp.kt`는 전면 재작성하지 않고 새 경로 기능부터
  `RouteFlowViewModel + SavedStateHandle`과 화면별 파일로 분리한다.

이유:

- 현재 Android는 도착 시각 없이 내부 24시간 예산을 사용하므로 새 제품 가치를 검증하지 못한다.
- 최대 5곳, 필수 조건 단계와 평균 머무름은 `늦지 않게 한 곳 들르기`라는 핵심 판단을 흐린다.
- 상대 분 올림은 15분 경계와 느린 요청에서 안전하지 않다.
- 운영 DB의 운영시간 보유율이 0.9%라 장소 수 확대보다 추천 신뢰를 먼저 확보해야 한다.
- 일반 경로 주변 추천은 기존 지도·내비 서비스와 직접 경쟁하므로 최대 체류시간과 출발
  권장시각이 첫 화면에서 드러나야 한다.

영향:

- 2026-08-26 스펙은 절대 마감·1곳 알파 기준으로 개정한다.
- 기존 `2026-08-26-deadline-aware-route-core.md` 구현 계획은 새 계획이 작성될 때까지 중지한다.
- 이전 `경유지 n/5`, 5곳 선택과 별도 필수 CONDITIONS를 신규 UI에 계승하지 않는다.
- 고정 출발 알림은 1차에 유지하고 지오펜스·두 번째 장소·원격 저장 동기화는 검증 후로 미룬다.

다시 검토할 조건:

- 한 곳 추천의 이해율, 선택률, 외부 내비 실행률과 거짓 가능 판정률이 알파 기준을 통과할 때
  두 번째 장소를 별도 실험한다.
- 운영시간·주차·대기 데이터가 쌓이면 공통 최소 15분에 장소별 내부 의미 체류시간을 추가한다.

## 2026-08-26 — 도착 마감 기반 경유지 추천

상태: 확정

결정:

- 사용자는 기존 위치 화면에서 목적지와 반드시 도착해야 하는 시각을 입력한다.
- 사용자는 체류시간을 입력하지 않는다.
- 안전 여유 10분과 장소당 최소 체류 15분은 시스템 내부값으로 관리한다.
- 지도 핀에는 추가 이동시간만 표시하고 장소 선택 후 최대 체류시간과 출발 마감 시각을 표시한다.
- 기본 경유지는 한 곳이며 실제 가능한 경우에만 두 번째 장소를 약하게 제안한다.
- 제품상 경유지는 최대 2곳이다.
- 고정 출발 알림을 기본으로 하고 위치 기반 도착 감지·재계산은 선택 기능으로 둔다.
- 실제 주행은 외부 카카오맵이 담당하며 틈새는 이동 상태의 지속 추적을 약속하지 않는다.

이유:

- 경유지의 실제 체류시간은 사용자마다 달라 사전 입력이나 임의 기본값을 적용하면 불필요한 단계와 오판이 생긴다.
- 도착 마감과 이동시간으로 가능한 체류 범위를 계산하면 입력을 늘리지 않고도 늦지 않기 위한 정보를 줄 수 있다.
- 세 곳 이상은 약속 시간 보호보다 일정 계획에 가까워져 현재 서비스 목적과 맞지 않는다.
- 외부 내비 사용 중 우리 앱이 실제 이동을 확실하게 알 수 없으므로 고정 알림이 더 신뢰할 수 있는 기본값이다.

영향:

- 활성 Android는 내부 `extraTimeMinutes=1,440` 대신 실제 남은 전체 시간인 `deadlineMinutes`를 사용한다.
- 신규 시간 모델은 서버 호환 분기로 추가하고 구버전 요청은 유지한다.
- 2026-08-16의 제품상 최대 5곳 결정은 최대 2곳으로 대체한다. 서버와 Kakao 전달 계층의 5곳 호환은 유지할 수 있다.
- 기존 `평균 머무름`과 미리 합산한 체류시간 표시는 제거 대상이다.

다시 검토할 조건:

- 사용자 검증에서 두 번째 경유지 이용률과 완료율이 충분히 높을 때만 세 곳 이상을 별도 일정 계획 기능으로 검토한다.
- 백그라운드 위치 권한 심사와 개인정보 고지가 준비되지 않으면 도착 감지는 출시하지 않고 고정 알림만 유지한다.

## 2026-08-16 — 단일 기준 저장소

상태: 확정

결정:

- Android, 백엔드, 다운로드 페이지와 문서를 `minjaeimnydaa/tteumsae` 한 저장소에서 관리한다.
- 최상위 폴더는 `android/`, `backend/`, `download/`, `design/`, `docs/`를 사용한다.
- 다른 작업 폴더와 과거 ZIP은 기준 원본으로 사용하지 않는다.

이유:

- 기존 Git 문서는 `0.10.1`, 실제 Android는 `0.12.3`으로 분리돼 있었다.
- 백엔드도 별도 저장소의 미커밋 변경과 인수 스냅샷이 갈라져 있었다.

영향:

- 기능 변경과 문서를 같은 커밋에서 검토할 수 있다.
- 배포 링크는 기록하되 APK 바이너리는 Git에 넣지 않는다.

## 2026-08-27 — 운영 Git 저장소 이전

상태: 확정

결정:

- 기존 `minjaeimnydaa/tteumsae`는 upstream 원본으로 유지한다.
- 직접 관리하고 배포할 기준 저장소는 `Newrred/tteumsae` 포크로 이전한다.
- 당시 통합 브랜치는 `agent/new-route-flow-ui`였으며, Vercel 백엔드의 Git 루트는 `backend/`로 설정했다.
- 당시 Vercel 프로젝트 `tteumsae-backend`는 통합 완료 전까지 해당 브랜치를 Production Branch로 사용했다. 2026-08-28 Gate 2 통합 뒤 공식 Git 기준과 Production Branch는 `main`으로 전환한다.
- CLI 배포 시 Android 산출물이나 로컬 작업물이 전송되지 않도록 저장소 루트 `.vercelignore`를 유지한다.

이유:

- 현재 운영 계정 `Newrred`는 원본 저장소에 읽기 권한만 있어 직접 push할 수 없다.
- 소스, 백엔드 배포와 운영 계정의 소유권을 한 계정 아래에서 관리한다.

## 2026-08-16 — 경유지 선택 표시 역할

상태: 폐기 — 2026-08-28 한 곳 알파 결정으로 대체

결정:

- 후보 카드 순서는 `장소 1/20`처럼 표시한다.
- 선택 현황은 `경유지 2/5`처럼 별도로 표시한다.
- 지도 선택 핀에는 실제 방문 순서 `1`, `2`, `3`을 표시한다.
- 카카오맵 전달 경유지는 최대 5개다.

이유:

- `1/20`만 표시하면 후보 위치와 선택 개수를 혼동할 수 있다.

영향:

- 최신 결과 화면 상단 카드에는 경유지 배지가 필요하다.

## 2026-08-13 — 장소 후보 핀

상태: 확정

결정:

- 기본 후보 핀은 파란색 장소 유형 아이콘을 사용한다.
- 선택 후보는 브랜드 빨간색 테두리와 경유 순서 배지를 사용한다.
- 별점 데이터는 현재 제공하지 않으므로 지도 핀에 가짜 별점을 표시하지 않는다.

이유:

- TourAPI 카테고리는 이미 있어 추가 API 없이 장소 유형을 구분할 수 있다.
- 실제 데이터가 없는 평점 표시는 신뢰를 해친다.

## 2026-08-13 — 복수 경유지 시간 표기

상태: 확정

결정:

- 앱의 복수 경유지 총시간과 남는 시간은 `예상`으로 취급한다.
- 최종 실제 경로와 시간은 카카오맵이 다시 계산한다.

이유:

- 현재 Android는 단일 후보별 우회·머무름을 합산한다.
- 전체 경유지를 Kakao Mobility로 재조회하지 않는다.

다시 검토할 조건:

- 백엔드에 복수 경유지 통합 경로 API가 구현될 때.

## 2026-08-13 — 브랜드 색상

상태: 확정

결정:

- 공식 Primary는 `#E60F33`이다.
- 은은한 그림자와 비활성 파생색은 Primary의 투명도 또는 명시된 토큰을 사용한다.

영향:

- 임의의 `#EF003D` 등 유사 빨간색을 새로 추가하지 않는다.

## 2026-08-03 — 이동 탐색의 이동수단

상태: 확정

결정:

- `경로 따라 갈 장소`는 자동차를 전제로 한다.
- `근처에서 갈 장소`만 도보와 자동차를 선택한다.

이유:

- 이동 탐색은 카카오맵 자동차 경유지 안내로 연결한다.
- Kakao Mobility 일반 길찾기는 보행 경로 시간을 제공하지 않는다.

## 2026-08-03 — 서버 키 경계

상태: 확정

결정:

- TourAPI 서비스키, Kakao REST 키와 Supabase Service Role 키는 Vercel에만 둔다.
- Android에는 Kakao Native App Key와 Supabase URL·publishable key만 빌드 시
  주입한다. publishable key의 권한 경계는 RLS다.

영향:

- 실제 키를 Git, `.env.example`, 문서나 Android 소스에 넣지 않는다.

## 2026-08-28 — Gate 1-B 데이터·운영 신뢰 경계

상태: 확정

결정:

- Kakao Mobility는 KST 일일 7,000건부터 경고하고 8,000건에서 외부 호출 전에 차단한다.
- 공급자 호출 예약·성공·쿼터·5xx·timeout은 좌표와 사용자 식별자 없이 일별 집계한다.
- 운영시간을 완전히 해석하지 못하면 `OPEN`이 아니라 `UNKNOWN`으로 남긴다.
- 축제는 KST 예정 방문일이 완전한 시작일·종료일 범위 안일 때만 추천한다.
- TourAPI 원문과 수동 검수를 분리하고 `place_curations`가 `effective_places`에서 우선한다.
- 첫 운영 검수 범위는 강릉 활성 장소 100곳이며 근거 없는 값은 추정하지 않는다.
- 공개 health 계약은 유지하고 상세 운영 집계는 `CRON_SECRET` 인증 경로로 분리한다.

이유:

- 무료 Kakao 쿼터를 요청 처리 중 뒤늦게 발견하면 핵심 경로 기능 전체가 갑자기 멈춘다.
- 영업정보가 없거나 복잡한 장소를 열림으로 추정하는 것이 후보 수 부족보다 더 큰 신뢰 손상이다.
- 전체 TourAPI 원문을 사람이 덮어쓰면 다음 동기화에서 근거와 수정 이력이 사라진다.
- 전국 전체 보강보다 실제 알파 지역의 얇고 검증 가능한 기준선이 Gate 2 판단에 유용하다.

영향:

- 예산·검수·운영 집계는 migration 006과 service-role 전용 RPC를 통해 관리한다.
- 예산/쿼터 소진은 503과 `Retry-After`로 반환하고 직선거리 대체 경로를 만들지 않는다.
- 복잡한 운영시간은 추천에 남을 수 있지만 `UNKNOWN`으로 명시되며 닫힘을 확정하지 않는다.
- 강릉 100곳 검수 확장은 Gate 3의 무결과율과 선택률을 확인한 뒤 결정한다.

다시 검토할 조건:

- Kakao 계약 쿼터나 유료 플랜이 바뀔 때 경고·차단선을 재산정한다.
- 운영시간 원문 패턴별 실제 오판 데이터가 쌓이면 파서 범위를 테스트와 함께 확장한다.

## 2026-08-28 — 핵심 흐름 UI 사용성 기준

상태: 확정

결정:

- 도착 마감은 별도 다이얼로그 대신 LOCATION 안의 24시간제 시·분 휠에서 즉시 바꾼다.
- 로딩은 지도 위 모달에서 일반화된 세 단계의 진행·완료 상태로 표현한다.
- 결과 추천은 지도와 함께 비교할 수 있는 드래그형 바텀시트로 제공한다.
- 없는 장소 정보는 추정하거나 `null`로 노출하지 않고 하나의 확인 안내로 합친다.
- 프로필에서 현재 추천에 쓰지 않는 연령대·성별 편집은 숨기되 기존 저장값은 보존한다.

이유:

- 가장 자주 쓰는 시간 입력의 추가 depth를 없애고 결과 비교 중 지도 맥락을 유지한다.
- 팝업과 로딩이 배경에 붙은 장식처럼 보이지 않도록 상태와 레이어를 명확히 구분한다.
- 데이터가 없는 상태를 반복 행이나 추정 정보로 채우면 장소 정보 신뢰가 낮아진다.

다시 검토할 조건:

- 사용성 테스트에서 분 단위 휠 조작 오류가 의미 있게 관찰될 때 minute step이나 빠른 선택을 검토한다.
- 연령대·성별이 실제 추천·개인화에 사용되고 사용자 효용이 검증될 때 프로필 노출을 재검토한다.

## 2026-08-29 — 홈 검색과 지도 패널의 연속성

상태: 확정

결정:

- HOME→LOCATION을 좌우 페이지 전환으로 만들지 않고 동일 지도 위 검색 패널의
  확장·부분 접힘 상태로 표현한다.
- 패널 접힘은 홈 복귀가 아니며 입력값과 검색 결과를 유지한다.
- 후보 개수는 `N곳`, 우회시간은 `+N분`으로 표기해 의미를 분리한다.
- 선택 후보는 클러스터에서 제외하고 자동 확대하며 전체 경로 복귀 수단을 제공한다.

이유:

- 페이지 전환과 지도 패널을 동시에 적용하면 지도가 교체되는 느낌과 중복 전환이 생긴다.
- 검색 목록, 시간 휠과 패널 드래그의 제스처 충돌을 피하려면 손잡이를 명시해야 한다.
- 출발·목적 대형 캡슐과 겹침 경쟁으로 숨은 후보는 실제 추천 개수를 오해하게 만든다.

## 미결정 목록

다음 항목은 확정된 결정이 아닙니다.

- 결과 카드에서 거리·통행료를 노출할지
- 붉은 후보 영역을 실제 corridor로 개선할지 제거할지
- 저장 탭을 전체 둘러보기와 분리할지
- `com.tteumsae.app` 패키지명을 Play 출시까지 유지할지

## 2026-08-27 — 선택 로그인과 최소 프로필

상태: 확정

결정:

- 게스트 기능을 기본으로 유지하고 카카오·Google 로그인은 저장 연속성이 필요한
  사용자에게만 제안한다.
- 공개 프로필 DB에는 이메일을 복제하지 않고 닉네임·이미지와 선택 연령대·성별만
  저장한다. 연령대·성별은 현재 추천에 사용하지 않는다.
- 운영자 표기는 `신홍`, 문의 메일은 `godburgundy@gmail.com`을 사용한다.
- 계정 삭제는 검증된 토큰 ID만 사용하고 Auth 사용자 삭제 cascade로 프로필과
  원격 저장 행을 함께 삭제한다.

## 2026-08-28 — Gate 1-A 무료 운영 런타임 안전 경계

상태: 확정

결정:

- Vercel Hobby의 카탈로그·intro Cron은 단일 오케스트레이터로 합치지 않고 UTC
  시간대를 4시간 분리한다.
- 수동 호출과 비정상 중복은 `sync_state` 기반 90초 DB lease와 service-role 전용
  claim/finish RPC로 차단한다.
- Supabase·Kakao·TourAPI 요청별 timeout과 추천 25초, Cron 신규 작업 45초·강제
  중단 50초의 내부 deadline을 둔다.
- 자동차 추천의 실제 Kakao Mobility 후보 호출은 최대 8개로 제한한다.

이유:

- Hobby Cron은 같은 시각대 안에서 최대 ±59분 오차가 있어 분 단위 순서에 의존할 수 없다.
- 현재 Vercel Function 제한은 60초지만 외부 요청과 DB 작업에 timeout이 없어 플랫폼
  강제 종료 전에 상태를 저장한다는 보장이 없다.
- 무료 운영에서 후보 20개 경로 호출은 비용·지연 대비 검증 가치가 낮다.

다시 검토할 조건:

- 유료 Vercel로 전환해 분 단위 Cron·긴 Function 또는 durable workflow를 사용할 때.
- Gate 3 실제 경로 검증에서 후보 8개로 인한 의미 있는 추천 누락이 확인될 때.

## 2026-08-29 — 화면 거리 기반 후보 묶음과 선택 카드 선두 배치

상태: 확정

결정:

- 지도 후보는 고정 미터 반경이 아니라 현재 줌에서 투영한 화면 거리로 묶고, Android
  기기 밀도를 반영한 `56dp`를 기본 상호작용 반경으로 사용한다.
- 클러스터 탭은 중심을 유지하며 3단계씩 확대해 반복 탭으로 개별 핀까지 분리한다.
- 사용자가 고르기 전 첫 후보를 지도에서 자동 포커스하지 않는다.
- 개별 핀 선택 시 선택 후보를 결과 목록 첫 항목으로 옮기고 선택 전보다 지도를 축소하지 않는다.

이유:

- 동일한 미터 반경은 줌아웃에서 핀이 겹치거나 줌인에서도 지나치게 오래 묶이는 문제를
  동시에 해결하지 못한다.
- 접힌 결과 시트에서는 단순 스크롤 명령이 기존 보이는 항목을 앵커로 유지할 수 있어,
  선택 항목 자체를 선두로 재배치하는 편이 지도와 목록의 대응을 확실하게 보장한다.

다시 검토할 조건:

- 태블릿·저밀도 기기에서 `56dp` 반경이 과도하거나 부족하다는 실기기 증거가 쌓일 때.
- 결과 순서 자체를 고정해야 하는 랭킹 설명 UI를 도입할 때.

## 2026-08-29 — 줌 방향별 후보 묶음 경계와 지도 안전 영역

상태: 확정

결정:

- 후보 묶음은 확대 `48dp`, 중립 `56dp`, 축소 `64dp`의 방향별 경계를 사용한다.
- 후보 간 묶음은 모든 구성원이 서로 경계 안에 있는 완전 연결 조건으로 만든다.
- 결과 지도 카메라는 상단 헤더와 하단 시트를 제외한 패딩 영역을 기준으로 움직인다.
- 클러스터 탭은 구성원 전체를 여백 있게 맞추며 회당 최대 2단계만 확대하고, 개별 핀
  선택은 줌 12로 맞춰 주변 표식까지 남긴다.

이유:

- 단일 경계는 손가락 줌이 멈추는 지점에서 결합과 분리가 반복될 수 있고, 중심점 기반
  묶음은 멀리 떨어진 양 끝 후보를 중간 후보 하나로 연결할 수 있다.
- 오버레이를 제외하지 않은 카메라 맞춤은 좌표상 화면 안에 있는 라벨도 헤더나 시트
  뒤에 숨길 수 있다.

다시 검토할 조건:

- 후보 수가 크게 늘어 공간 인덱스가 필요하거나, 기기별 지도 유효 영역을 시트의 실제
  높이와 실시간으로 연동해야 할 때.

## 2026-09-05 — 실기기 전 화면 감사 기반 상용화 UI 계약

상태: 확정

결정:

- HOME·LOCATION·LOADING·RESULTS, 추천 상세, 장소 탐색·상세, SETTINGS·로그인·PROFILE과
  주요 모달을 실기기 화면 캡처, UI 계층과 실제 이동 흐름을 함께 보는 하나의 감사 범위로
  관리한다.
- 지도 화면은 full-bleed로 렌더링하고, Scaffold의 content inset과 오버레이 inset을
  중복하지 않는다. 일반 목록·폼은 Scaffold 안전 영역을 한 번만 사용한다.
- 첫 안내가 사라져도 HOME에는 서비스 가치 문구를 유지하고, 현위치 재탭은 표시 해제가
  아니라 위치 재획득·재중심으로 동작한다.
- LOCATION의 CTA는 부족한 입력 조건을 설명하고 검색 지우기를 제공한다. 필터와 주요
  조작은 최소 `48dp` 및 명시적 선택 semantics를 갖는다.
- 로딩과 확인창은 실제 모달로 바깥 입력을 차단한다. 진행·완료·오류는 live region과 상태
  설명을 제공하며, 지도 타일이 불완전한 동안에는 스켈레톤을 유지한다.
- RESULTS는 불투명한 시트·카드와 재시도 가능한 경고 배너를 사용한다. 후보 선택은 랭킹을
  바꾸지 않고 안정적인 목록 상태에서 해당 카드로 스크롤한다. 이 결정은 2026-08-29의
  `선택 카드를 목록 선두로 재배치` 정책을 대체한다.
- 추천 상세의 최대 체류·출발 시각은 현재 교통 스냅샷에 따른 비보장 권장값으로 표현하고,
  계산 시각과 변동 가능성을 함께 표시한다. 운영·휴무·설명 등 핵심 텍스트는 말줄임표로
  자르지 않는다.
- 장소 탐색은 adaptive grid와 행동 가능한 빈 상태를 사용한다. 저장 상세에는 저장 토글,
  실제 방문 정보와 정확한 `카카오맵에서 보기` 행동을 제공한다.
- 같은 지역의 성공한 카탈로그는 화면 재진입 때 유지하고, 지역 변경이나 명시적 재시도에서
  무효화한다. 탭·상세 왕복만으로 목록을 다시 스켈레톤 상태로 만들지 않는다.
- 설정·계정·프로필은 일반 콘텐츠 최대 폭, `48dp` 이상 조작 영역과 상태 semantics를
  공유한다. 준비 중 약관은 비활성으로 표시하고, 계정 삭제·캐시·저장 확인창은 불투명한
  surface, 제한된 폭과 명확한 확인·취소 위계를 사용한다.

이유:

- 화면별 실기기 캡처에서 기능 자체보다 중복 inset, 덜 그려진 지도, 배경이 비치는 surface,
  작은 터치 영역, 화면 밖 오류와 잘린 텍스트가 제품 완성도와 신뢰를 크게 낮췄다.
- 지도 앱의 배경 맥락을 유지하면서도 입력 가능한 레이어를 명확히 분리해야 화면 이동이
  자연스럽고, TalkBack·큰 글자·넓은 화면에서도 같은 상태를 이해할 수 있다.
- 추천 순서를 선택 때문에 바꾸면 랭킹의 의미가 흔들린다. 시각적 연결은 순서 변경보다
  선택 위치 스크롤과 지도 초점으로 해결한다.
- 재진입마다 성공한 카탈로그를 버리면 불필요한 네트워크 요청, 스켈레톤 점멸과 목록
  맥락 손실이 반복된다.

다시 검토할 조건:

- 사용자 조사에서 홈의 영속 가치 문구가 지도 탐색을 방해하거나 CTA 이해에 도움이 되지
  않는다는 증거가 쌓일 때.
- 자동 새로고침이나 서버 캐시 무효화 정책을 도입해 카탈로그 신선도 계약이 바뀔 때.
- 태블릿·폴더블 전용 정보 구조 또는 Navigation Compose 전환을 별도 범위로 승인할 때.

이 결정은 전체 단위 테스트 132/132, lint 오류 0, 기본 글자 크기 시각 회귀 3/3과
글자 배율 1.3·2.0 핵심 경로, 위치 권한 거부·카카오맵 실제 전환을 통과했다. 변경 후
실기기 화면 21장을 재캡처했고 서명된 release APK까지 확인했다. 확정 수치와 남은
호환성 행렬은 `docs/08_QA_AND_KNOWN_ISSUES.md`에 기록한다.

## 2026-09-05 — 결과 지도·목록·상세 V2

상태: 확정

결정:

- 지도 후보는 추가 이동시간 숫자 대신 카테고리 그림 핀을 사용하고, 숫자는 목록에서 비교한다.
- 묶음은 대표 카테고리를 빈도순으로 보여준다. 클릭 시 `N-1 + 1` 단계는 건너뛰고,
  모든 좌표를 안전 영역에 둔 채 클릭한 묶음의 화면거리 임계값을 낮춰 복수 하위 그룹을 드러낸다.
- 장소 선택 중에는 다른 후보 핀을 숨겨 선택 경로와 핀을 우선한다. 목록 순서는 유지하고
  선택 카드가 펼쳐진 뒤에도 해당 행을 한 번 더 상단에 맞춘다.
- 결과 목록에서 `UNKNOWN` 운영상태는 경고 태그로 반복하지 않는다. 실제로 운영 가능함이
  계산된 경우에만 운영 태그를 표시한다.
- 상세는 단건 API에서 전화·홈페이지·소개·사진·운영시간·휴무·주차·행사 기간을 다시 읽고,
  값이 있는 섹션만 표시한다. 긴 휴무 규칙은 항목별 줄로 나누고 출처·검토일을 함께 표시한다.
- 상세 단건 요청에 실패하면 경로 결과 스냅샷을 유지하고 재시도를 제공한다.

이유:

- 지도만 보고 후보 종류와 공간 관계를 판단할 수 있어야 하며, 겹친 핀을 형식적으로 한 개씩
  벗겨내는 동작은 사용자가 장소 분포를 이해하는 데 도움이 되지 않는다.
- 확인되지 않은 운영정보를 모든 행에서 경고하면 실제 경고와 확인된 정보의 위계가 사라진다.
- 방문 판단에는 일반 추천 문구보다 현재 경로의 체류 가능시간과 실제 운영·휴무·주차 정보가 유용하다.

세부 계약과 후속 API 범위는 `docs/13_RESULTS_MAP_DETAIL_V2.md`를 따른다.

이 결정은 Android 단위 테스트 142/142, Backend 테스트 166/166, lint 오류 0,
서명 release 빌드와 기본 글자 크기 시각 회귀 3/3을 통과했다. 동일 실기기에서 글자 배율
1.3·2.0 핵심 경로도 통과했으며, 운영 백엔드 배포 후 health와 장소 단건 상세 응답을 확인했다.
